# M02: 用图表推理样本理解“看见数值”和“做出判断”的差别

## 背景

图表任务往往不是读取一组数字，而是要从视觉表达中做比较、排序、异常判断。这和普通文本问答有明显差异。

## 任务目标

- 建立图表推理的最小数据与评测样本
- 展示 text-only 与 vision-augmented 的结果差距
- 为后续 dashboard、BI、多模态分析任务打基础

## 交付标准

- 至少有一个 chart reasoning 任务
- 至少支持成功率对照
- 至少说明视觉字段和最终判断之间的关系
- 最好能覆盖表格数值被 OCR 污染时的失败样本
