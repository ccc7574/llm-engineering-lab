{"task_id":"reverse_string","instruction":"Complete the function so it returns the reversed string.","starter_code":"def reverse_string(text: str) -> str:\n    # TODO: implement\n    raise NotImplementedError","entry_point":"reverse_string","canonical_solution":"def reverse_string(text: str) -> str:\n    return text[::-1]","tests":["assert reverse_string('abc') == 'cba'","assert reverse_string('') == ''","assert reverse_string('level') == 'level'"]}
{"task_id":"sum_positive","instruction":"Complete the function so it returns the sum of only the positive integers in values.","starter_code":"def sum_positive(values: list[int]) -> int:\n    # TODO: implement\n    raise NotImplementedError","entry_point":"sum_positive","canonical_solution":"def sum_positive(values: list[int]) -> int:\n    total = 0\n    for value in values:\n        if value > 0:\n            total += value\n    return total","tests":["assert sum_positive([1, -2, 3, 0]) == 4","assert sum_positive([-5, -1]) == 0","assert sum_positive([10, 20]) == 30"]}
{"task_id":"normalize_ticket_id","instruction":"Complete the function so it trims surrounding spaces, uppercases the text, and replaces internal spaces with hyphens.","starter_code":"def normalize_ticket_id(text: str) -> str:\n    # TODO: implement\n    raise NotImplementedError","entry_point":"normalize_ticket_id","canonical_solution":"def normalize_ticket_id(text: str) -> str:\n    cleaned = text.strip().upper()\n    return '-'.join(cleaned.split())","tests":["assert normalize_ticket_id(' inc 42 ') == 'INC-42'","assert normalize_ticket_id('bug  7') == 'BUG-7'","assert normalize_ticket_id('ticket') == 'TICKET'"]}
